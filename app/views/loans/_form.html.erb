<%= form_for(loan) do |f| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :loan_date %>
    <%= f.date_select :loan_date %>
  </div>

  <div class="field">
    <%= f.label :return_date %>
    <%= f.date_select :return_date %>
  </div>

  <div class="field">
    <%= f.label :book_id %>
<%= f.select :book_id, Book.all.collect {|p| [ p.name, p.id ] }%>
</div>

  <div class="field">
    <%= f.label :user_id %>
<%= f.select :user_id, User.all.collect {|p| [ p.email, p.id ] }%>
  </div>

  <div class="actions">
    <%= f.submit 'Create New Loan', :class => 'btn btn-default' %>
  </div>
<% end %>
